# MedSpan dependencies — verified to match Google Colab T4's pre-installed
# stack (Python 3.12, CUDA 12.1) as of 2026. On Colab the install cell
# becomes a no-op; locally these are the versions we test against.
#
# torch is intentionally unpinned so Colab's CUDA build is preserved. On
# a local machine, install the matching variant from pytorch.org first.
torch>=2.2.0

transformers==4.46.3
accelerate==1.1.1
datasets==2.21.0
tokenizers==0.20.3
evaluate==0.4.3

gradio==4.44.0

numpy==1.26.4
pandas==2.2.2
scikit-learn>=1.4.2
tqdm>=4.66.2

ipywidgets>=8.1.2
