# MedSpan dependencies — pinned to a transformers 4.44 stack because Colab T4
# (as of 2026) ships torch 2.2.2, and transformers 4.45+ requires torch 2.4+
# (calls `torch.library.register_fake`, which doesn't exist in torch 2.2).
#
# torch is intentionally unpinned: Colab ships its own CUDA build that we
# must NOT overwrite. Locally, install the right torch variant from
# pytorch.org first.
torch>=2.2.0

transformers==4.44.2
accelerate==0.34.2
datasets==2.21.0
tokenizers==0.19.1
evaluate==0.4.3

gradio==4.44.0

numpy==1.26.4
pandas==2.2.2
scikit-learn>=1.4.2
tqdm>=4.66.2

ipywidgets>=8.1.2
